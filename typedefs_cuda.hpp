#pragma once

#include <cuComplex.h>
#include <cuda_runtime.h>

// using Idx = long int;
// using Idx = long long int;
using Idx = unsigned long;
using Complex = cuDoubleComplex;

// const Complex I = make_cuDu(0, 1);
